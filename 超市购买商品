//顾客在超市购买商品。已知某种糖果1.5元/斤，输入顾客购买斤数(可以有不是整斤的情况)
//和顾客支付金额（假设不会出现支付金额<应付金额的情况），计算应付金额和找补金额
//（涉及到小数输出时，全部保留2位)
#include<stdio.h>
main()
{
    float a=1.5,b,c;
    printf("购买斤数= ");
    scanf("%f",&b);
     printf("支付金额= ");
    scanf("%f",&c);
    float sum,x;
    sum=a*b;
    x=c-sum;
    printf("找补金额%.2f",x);
}
